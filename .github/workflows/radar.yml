name: KURAMA MARKET RADAR

on:
  schedule:
    - cron: "15 * * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  radar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: レーダー更新
        run: python kurama_radar.py --out docs/index.html --notify
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      - name: コミット
        run: |
          git config user.name "kurama-radar-bot"
          git config user.email "bot@users.noreply.github.com"
          git add docs state.json
          git commit -m "radar update $(date -u +%Y-%m-%dT%H:%M)" || echo "変更なし"
          git push
